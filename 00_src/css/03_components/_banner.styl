.banner
    height 80vh
    min-height 40rem
    background-size 300%

    &__inner
        position relative
        height 100%

    &__title
        position absolute
        left $gap
        bottom 4rem

        +tablet()
            left (2 * $gap)
            bottom (2 * $gap)
